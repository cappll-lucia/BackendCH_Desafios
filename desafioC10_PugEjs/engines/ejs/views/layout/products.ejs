<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@4.6.0/dist/css/bootstrap.min.css"
        integrity="sha384-B0vP5xmATw1+K9KRQjQERJvTumQW0nPEzvF6L/Z6nronJ3oUOFUFpCjEUQouq2+l" crossorigin="anonymous">
    <title>Productos</title>
</head>
<body class="bg-dark">
    <h2 class="text-light m-3 me-auto">Productos Registrados</h2>
    <% if(products.length > 0){ %>
        <div class="table-responsive">
            <table class="table table-dark">
                <tr> <th class="text-center text-light">ID</th> <th class="text-center text-light">Nombre</th> <th class="text-center text-light">Precio</th> </tr>
                <% for (const prod of products) {%>
                <tr>
                    <td class="text-center text-light">#<%=prod.id%></td>
                    <td class="text-center text-light"><%=prod.title%></td>
                    <td class="text-center text-light">$<%=prod.price%></td>
                </tr>
                <%};%>
            </table>
        </div>
    <% } else{ %>
        <h3 class="alert alert-danger">no se encontraron productos registrados</h1>
    <% } %>
    <a href="./"><button class="btn btn-outline-light">Ir a la carga de productos</button></a>
</body>
</html>